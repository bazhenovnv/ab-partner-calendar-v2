# Document Conflict Matrix

| Topic | Document A / statement | Document B / statement | Winning source | Correction |
|---|---|---|---|---|
| Repository | Current work sometimes references `ab-partner-calendar` | Current project is `ab-partner-calendar-v2` | `00_SOURCE_OF_TRUTH.md` | Old repository is historical and forbidden for current work |
| Canonical branch | Historical docs name `claude/ab-afisha-architecture-plan-805f5o` or `codex/events-modal-carousel` as the working branch | Repository default/integration branch is `main`; design work uses short-lived feature branches from current `main` | repository metadata + approved workflow + `00_SOURCE_OF_TRUTH.md` | Use `main` as the sole canonical integration branch; preserve old branch names only as history |
| VPS IP | Main TZ lists `77.232.136.248` as production VPS | Current deployment uses `5.129.243.179` | current infrastructure + ADR-014 | Mark old IP historical; use current IP only in deployment docs |
| Runtime IP usage | Old docs contain literal server IP | ADR-014 requires domain-first runtime | ADR-014 | Runtime uses domains; IP appears only in deployment documentation |
| Contact email | Approved visual reference may show a legacy address | Current requirements use `info-event@a-b.ru` | BUSINESS_RULES BR-014 + current TZ additions | Runtime and legal docs use `info-event@a-b.ru`; legacy screenshot content is not copied |
| Hero CTA | Some reports/code use `Главные события` or other labels | Approved reference uses `Важные события →` | approved design + canonical design docs | Hero CTA is `Важные события →` |
| Carousel title | Older wording may use `Важные события` | Main section is `Главные события` | current TZ + approved design | Keep `Главные события` only for carousel heading |
| Completed status | Legacy wording `Проведено` | BR-009 requires `Завершено` | BUSINESS_RULES | Use `Завершено` everywhere |
| Design completion | Historical reports state `97%`, `complete` or `pixel-perfect` | Current acceptance evidence is incomplete | approved design + current staging + proof rules | Treat reports as historical evidence only |
| Frontend build | Some instructions say `docker compose build frontend` | Current compose/build architecture may require direct Docker build or current compose target | actual current deployment files | Follow `06_DEPLOYMENT_CURRENT.md`; do not copy historical commands without verification |
| Images | Stage work created custom SVGs, gradients or placeholders | User rule requires only approved assets | approved design + Asset Registry | Remove invented final assets; record missing approved assets |
| Missing event image | UI may show AB/gradient placeholder | BR-005 says missing image blocks publication | BUSINESS_RULES | Do not publish or accept cards without approved image |
| Main events membership | Ad hoc display logic | BR-001/BR-002 and current API require `mainEvent=true` | BUSINESS_RULES + carousel specification | Main carousel uses published `mainEvent=true` events only |
| Main events fallback | Older specification allowed completed featured events only when no active featured events existed | Current business rule fills remaining positions after active featured events | BUSINESS_RULES BR-034 | Select active featured events first, then fill remaining positions with completed featured events |
| Main event artwork fit | Older specification stated `object-fit: cover` | Current business rule and implementation require complete artwork without cropping | BUSINESS_RULES BR-034 + current implementation | Use `object-fit: contain`; verify approved source mapping before acceptance |
| Carousel indicators | Older specification stated one dot per event | Current approved-looking implementation uses three direction/current-position indicators | approved reference + current implementation pending visual proof | Do not describe controls as one dot per event; verify exact approved navigation during R6 |
| Legal pages | Older TZ may list fewer pages | BR-027 requires five legal routes | BUSINESS_RULES v11 | Footer and site expose all five routes |
| Calendar heading | Implementations may omit year | BR-019 requires month + year | BUSINESS_RULES | Always render month and year |
| Dropdown arrow | Custom decorative arrow behaviour | BR-020 defines open/closed rotation | BUSINESS_RULES | Use state-only chevron behaviour |
| Overall desktop size | Physical screenshot of Figma prototype appears narrower because viewer uses `scaling=min-zoom` | Raw Figma nodes and canonical docs define centered panels at `1496–1497px` in a `1920px` CSS viewport | raw Figma geometry + approved PDF + `09_PAGE_CANVAS_AND_SCALE.md` | Never copy Figma viewer zoom into CSS; record viewport and devicePixelRatio before comparison |
| Main page background | Figma viewer shows black side areas around the prototype | Approved website canvas is light grey `#F1F1F1` with white panels | raw design frame + `02_DESIGN_SYSTEM.md` + `09_PAGE_CANVAS_AND_SCALE.md` | Do not add black side bars; black areas are viewer chrome |
| Historical visual QA | Historical comparison files contain statuses that later changed | Current branch/staging may differ | current code + current staging + canonical specs | Treat old comparison status rows as historical |
| Canonical desktop scale | Some reports attempted to shrink widths after comparing to a min-zoom viewer | Recorded Figma geometry defines current canonical widths | raw Figma node geometry + `09_PAGE_CANVAS_AND_SCALE.md` | Do not reduce canonical max-width values without new approved measurements |
| Global background | Older CSS drafts applied white to full-width section wrappers | Required canvas is `#F1F1F1`; panels are white; section wrappers are transparent | desktop canvas specification | Do not add conflicting full-width white wrappers |
| Figma editor workspace | Medium gray outside the selected frame was confused with the page canvas | Page canvas is the fill inside the selected frame | direct design-frame sampling | Never implement editor workspace colour |
| MAX schedule | Historical notes treated webhook as the only production path | Active requirements include hourly reconciliation | active TZ + BUSINESS_RULES | Keep webhook for immediate updates and hourly reconciliation through the same importer |
| MAX collection posts | Old code silently skipped weekly/multi-event posts | BR-003 requires `NEEDS_ATTENTION` without automatic splitting | BUSINESS_RULES BR-003 | Persist one source record as `NEEDS_ATTENTION` and notify administration |
| MAX publication | Old code stored every valid import as `DRAFT` | Complete imports must appear publicly | BUSINESS_RULES BR-001/BR-002 | Publish complete imports; incomplete imports remain `NEEDS_ATTENTION` |
| PROJECT_BIBLE numbering | Two canonical files used the prefix `09` | Canonical navigation requires unique identifiers | documentation stabilization decision | Keep desktop canvas as `09`; release checklist is `09B_RELEASE_ACCEPTANCE_CHECKLIST.md`; update all current cross-references |

## Status

This matrix records current known conflicts. Resolved conflicts remain here as governance history, but their correction must also exist in the winning canonical document. New conflicts must not be resolved silently.